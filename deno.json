{
  "tasks": {
    "build": "deno run -A scripts/build.ts",
    "dev": "deno test --allow-read",
    "check": "deno check src/mod.ts",
    "prepublish": "deno task check && deno task build"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  }
}
